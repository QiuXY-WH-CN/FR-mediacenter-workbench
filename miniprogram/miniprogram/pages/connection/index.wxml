<view class="page"><view class="title">连接设置</view><view class="hint">选择连接方式并保存即可，服务器相关配置由管理员完成。</view><view class="conn-options"><view class="conn-option {{mode === 0 ? 'selected' : ''}}" data-mode="0" bindtap="choose"><view class="h3">本机 / 局域网直连</view><view class="muted">开发者工具或同一 Wi-Fi 调试</view></view><view class="conn-option {{mode === 1 ? 'selected' : ''}}" data-mode="1" bindtap="choose"><view class="h3">微信云函数</view><view class="muted">手机跨网络使用</view></view></view><view class="field" wx:if="mode === 0"><text class="label">服务器地址</text><input value="{{directBaseUrl}}" data-key="directBaseUrl" bindinput="input" placeholder="http://127.0.0.1:8766"/><view class="muted">开发者工具填 127.0.0.1；手机同一 Wi-Fi 填电脑 IP。</view></view><view class="field" wx:else><text class="label">云开发环境 ID</text><input value="{{cloudEnv}}" data-key="cloudEnv" bindinput="input" placeholder="例如 cloud1-xxxxxx"/><view class="muted">填写管理员提供的环境 ID 后保存即可。</view></view><view wx:if="message" class="hint">{{message}}</view><view class="actions"><button loading="{{testing}}" disabled="{{testing}}" bindtap="test">测试连接</button><button class="primary" bindtap="save">保存并重新登录</button></view></view>
